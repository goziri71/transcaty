/**
 * Tekko NGN settlement pocket — native Naira ledger (not Europe USDC / PYUSD-USDC).
 */
export const NGN_SETTLEMENT_CURRENCY = "NGN" as const;
export const NGN_SETTLEMENT_DISPLAY_NAME = "NGN" as const;
