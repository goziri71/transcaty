/**
 * Tekko PYUSD settlement pocket — distinct from Europe Tylt `USDC`.
 * Display name is "PYUSD USDC"; ledger/API code is `PYUSD-USDC`.
 */
export const PYUSD_SETTLEMENT_CURRENCY = "PYUSD-USDC" as const;
export const PYUSD_SETTLEMENT_DISPLAY_NAME = "PYUSD USDC" as const;
export const EUROPE_SETTLEMENT_CURRENCY = "USDC" as const;
